# hoc - hoc unix port from plan9
# Depends on ../lib9

TARG      = hoc
OFILES    = y.tab.o init.o code.o math.o symbol.o
YFILES    = hoc.y
MANFILES  = hoc.1

include ../yacc.mk

pre-uninstall:

post-install:
